//Template Literals

let obj = {
    item: "pen",
    price:10,
};

let output = `The cost of ${obj.item} is ${obj.price} rupees.`
console.log(output);

let object= {
    name:"java script",

};
let wish = `I like so much ${object.name},you are best ${object.name} for me and in my life***`
console.log(wish);